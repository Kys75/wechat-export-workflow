# 来源与实现范围

本仓库是根据仓库拥有者授权整理的本地微信工作流重新实现的可分享版本。只阅读既有源代码、技能规范、知识卡及定向项目对话以确认需求，没有复制密钥、聊天内容、数据库、日志、用户标识或原项目的 Git 历史。

既有脚本目录未提供独立许可证；因此没有直接复制其源码，也没有擅自声明第三方代码可按 MIT 重新分发。本仓库暂不授予通用开源许可，第三方依赖遵循各自许可证。软件包依赖使用发行版安装，不内嵌第三方源码。

原工具 README 提到的技术参考项目：

- https://github.com/ylytdeng/wechat-decrypt （SQLCipher 内存密钥与数据库结构方面的参考；本仓库不内嵌该仓库代码）
- https://www.zetetic.net/sqlcipher/design/ （SQLCipher 数据格式与加密设计）
- https://gofastmcp.com/ （stdio MCP 接口依赖）

本次定向复核过历史项目对话中从其他 Agent 迁移 Skill、刷新失败却显示成功、应用升级/文件权限导致旧快照，以及后续群聊引用发送者修复的上下文。对外只保留可复用工程结论：

1. 列出会话后再导出，显示名不唯一时不猜测。
2. 刷新成功必须以实际解密和校验为依据，不能依赖固定文案或部分成功。
3. 会话可能跨多个消息分片，导出和搜索均需遍历。
4. `real_sender_id` 经本分片 `Name2Id.rowid` 恢复发送者；引用作者单独展示。
5. 内容先解压再搜索，不能对压缩 BLOB 使用 SQL LIKE 判断无结果。
6. 输出模式分清上下文读取、定向提取和完整注释；不能把完整记录偷偷压缩成节选。
7. 时间相邻不是回复关系；解读应明确区分原话、线程推断和外部事实。

验证范围：合成多分片 SQLite、压缩消息、引用、日期边界、只读查询、路径保护、失败刷新、stdio MCP 的客户端调用，以及安装了 SQLCipher 4 时的真实加解密回环。未用真实账户或进程重新提钥，未向任何外部服务发送私人数据。详见测试代码中的具体断言。
